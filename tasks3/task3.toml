extract = 'total_dyn_inst: (\d+)'
benchmarks = '/Users/gerardogtn/Documents/cornell/cs6120/tools/bril/benchmarks/core/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}",
]

[runs.lvn]
pipeline = [
    "bril2json",
    "./run.sh",
    "brili -p {args}"
]
